# 모든 텍스트 파일은 LF로 저장 (Git 기준)
* text=auto eol=lf

# 셸 스크립트나 유닉스 계열 파일은 LF 유지
*.sh text eol=lf

# Windows 배치 파일은 CRLF 유지
*.bat text eol=crlf
*.cmd text eol=crlf

# 자바, XML, 프로퍼티 등도 LF로 통일
*.java text eol=lf
*.xml text eol=lf
*.properties text eol=lf

# 바이너리 파일은 텍스트 처리하지 않음
*.png binary
*.jpg binary
*.exe binary
*.jar binary